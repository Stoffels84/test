# --- SMTP ---
SMTP_HOST=mail.borolo.be
SMTP_PORT=587
SMTP_USER=confirmatie@borolo.be
SMTP_PASS=22111984
EMAIL_FROM="Schade Dashboard <confirmatie@borolo.be>"
SMTP_SSL=false

# --- Toegestane domeinen ---
ALLOWED_EMAIL_DOMAINS=delijn.be,delijn-teambuiling.be

# --- OTP instellingen ---
OTP_LENGTH=6
OTP_TTL_SECONDS=600
OTP_RESEND_SECONDS=60

# --- Mail templates ---
OTP_SUBJECT=Je verificatiecode
OTP_BODY_TEXT=Beste {name}\n\nJe verificatiecode om in te loggen in schade is: {code}\nJe hebt {minutes} min om in te loggen.\n\nSucces,\nOneTeamGent
OTP_BODY_HTML=<div style="font-family:Arial,sans-serif"><h2>Je verificatiecode</h2><p>Beste {name},</p><p>Code: <strong>{code}</strong></p><p>Geldig: {minutes} min</p><p><small>{date} — PNR {pnr}</small></p><hr><p>#OneTeamGent</p></div>

# --- Contactenbron ---
# Laat deze twee regels WEG als je 'contact' tabblad in 'schade met macro.xlsm' gebruikt (dat ondersteunt je code al).
# CONTACTS_FILE=schade met macro.xlsm
# CONTACTS_SHEET=contact

# --- Alleen gebruiken als je GEEN e-mailkolom hebt en mails wilt bouwen als <PNR>@delijn.be ---
# FALLBACK_EMAIL_DOMAIN=delijn.be
